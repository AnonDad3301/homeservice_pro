<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Дашборд Мастера | HomeService Pro</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: <%= styles.primary %>;
            --secondary: <%= styles.secondary %>;
            --success: #4cc9f0;
            --warning: #ffb703;
            --danger: #e63946;
            --dark: #2b2d42;
            --light: #f8f9fa;
            --gray: #8d99ae;
            --card-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
            --transition: all 0.3s ease;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background-color: #f8f9fa;
            color: var(--dark);
            display: flex;
            min-height: 100vh;
        }

        .sidebar {
            width: 260px;
            background: linear-gradient(180deg, var(--dark) 0%, #1d1f30 100%);
            color: white;
            padding: 1.5rem 0;
            height: 100vh;
            position: sticky;
            top: 0;
            display: flex;
            flex-direction: column;
            box-shadow: var(--card-shadow);
            z-index: 100;
        }

        .brand {
            padding: 0 1.5rem 1.5rem;
            border-bottom: 1px solid rgba(255,255,255,0.1);
            margin-bottom: 1rem;
        }

        .brand h2 {
            display: flex;
            align-items: center;
            gap: 12px;
            font-size: 1.4rem;
        }

        .brand i {
            color: var(--success);
        }

        .nav-links {
            flex: 1;
            padding: 0 1rem;
        }

        .nav-links li {
            list-style: none;
            margin-bottom: 4px;
            border-radius: 8px;
            overflow: hidden;
        }

        .nav-links a {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 0.85rem 1rem;
            color: rgba(255,255,255,0.8);
            text-decoration: none;
            font-weight: 500;
            transition: var(--transition);
        }

        .nav-links a:hover, .nav-links a.active {
            background: rgba(255,255,255,0.1);
            color: white;
        }

        .nav-links i {
            width: 24px;
            text-align: center;
        }

        .content {
            flex: 1;
            padding: 2rem;
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
        }

        .welcome-card {
            background: linear-gradient(120deg, var(--primary), var(--secondary));
            color: white;
            border-radius: 16px;
            padding: 2rem;
            box-shadow: var(--card-shadow);
        }

        .welcome-card h3 {
            font-size: 1.8rem;
            margin-bottom: 0.5rem;
            font-weight: 600;
        }

        .welcome-card p {
            opacity: 0.9;
            max-width: 600px;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
            gap: 1.5rem;
            margin-top: 2rem;
        }

        .stat-card {
            background: rgba(255,255,255,0.15);
            backdrop-filter: blur(10px);
            border-radius: 12px;
            padding: 1.5rem;
            display: flex;
            align-items: center;
            gap: 1rem;
            transition: var(--transition);
        }

        .stat-card:hover {
            transform: translateY(-5px);
            background: rgba(255,255,255,0.2);
        }

        .stat-icon {
            width: 60px;
            height: 60px;
            border-radius: 12px;
            background: rgba(255,255,255,0.2);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.8rem;
        }

        .stat-info {
            flex: 1;
        }

        .stat-info span {
            font-size: 1.8rem;
            font-weight: 700;
            display: block;
            line-height: 1.2;
        }

        .card {
            background: white;
            border-radius: 16px;
            padding: 1.75rem;
            box-shadow: var(--card-shadow);
        }

        .card-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1.5rem;
        }

        .card-header h3 {
            font-size: 1.4rem;
            font-weight: 600;
            color: var(--dark);
        }

        .btn {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 0.7rem 1.25rem;
            background-color: var(--primary);
            color: white;
            text-decoration: none;
            border-radius: 8px;
            font-weight: 500;
            transition: var(--transition);
            border: none;
            cursor: pointer;
            font-size: 0.95rem;
        }

        .btn:hover {
            background-color: var(--secondary);
            transform: translateY(-2px);
        }

        .btn i {
            font-size: 0.9rem;
        }

        .data-list {
            margin-top: 1.5rem;
            border-radius: 12px;
            overflow: hidden;
            border: 1px solid #eee;
        }

        .list-header {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            background: #f8f9fa;
            padding: 1rem 1.5rem;
            font-weight: 600;
            border-bottom: 1px solid #eee;
        }

        .list-item {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            padding: 1rem 1.5rem;
            border-bottom: 1px solid #f0f0f0;
            transition: var(--transition);
        }

        .list-item:last-child {
            border-bottom: none;
        }

        .list-item:hover {
            background: #f9faff;
        }

        .status-badge {
            display: inline-block;
            padding: 0.25rem 0.75rem;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: 500;
        }

        .status-pending {
            background: #fff3e0;
            color: #ef6c00;
        }

        .action-buttons {
            display: flex;
            gap: 0.5rem;
        }

        .action-btn {
            background: transparent;
            border: none;
            cursor: pointer;
            font-size: 1rem;
            transition: var(--transition);
            width: 32px;
            height: 32px;
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .action-btn:hover {
            background: rgba(0,0,0,0.05);
        }

        .accept-btn { color: var(--success); }

        @media (max-width: 992px) {
            .list-header, .list-item { grid-template-columns: repeat(2, 1fr); }
        }

        @media (max-width: 768px) {
            body { flex-direction: column; }
            .sidebar { width: 100%; height: auto; position: relative; }
            .content { padding: 1.5rem; }
            .stats-grid { grid-template-columns: 1fr 1fr; }
        }

        @media (max-width: 576px) {
            .stats-grid { grid-template-columns: 1fr; }
            .list-header, .list-item { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>
    <aside class="sidebar">
        <div class="brand">
            <h2><i class="fas fa-tools"></i> HomeService Pro</h2>
        </div>
        <ul class="nav-links">
            <li><a href="/master/dashboard" class="active"><i class="fas fa-chart-line"></i> Дашборд</a></li>
            <li><a href="/master/orders"><i class="fas fa-clipboard-list"></i> Заявки</a></li>
            <li><a href="/master/payments"><i class="fas fa-money-bill-wave"></i> Оплаты</a></li>
            <li><a href="/master/profile"><i class="fas fa-user"></i> Профиль</a></li>
            <li><a href="/master/services/create"><i class="fas fa-plus"></i> Добавить услугу</a></li>
        </ul>
    </aside>

    <main class="content">
        <section class="welcome-card">
            <h3>Добро пожаловать, <%= user.firstName %>!</h3>
            <p>Просмотрите доступные заявки и управляйте своими услугами</p>
            
            <div class="stats-grid">
                <div class="stat-card">
                    <div class="stat-icon"><i class="fas fa-clipboard"></i></div>
                    <div class="stat-info"><span><%= orders.length %></span> Доступных заявок</div>
                </div>
                <div class="stat-card">
                    <div class="stat-icon"><i class="fas fa-concierge-bell"></i></div>
                    <div class="stat-info"><span><%= services.length %></span> Ваших услуг</div>
                </div>
            </div>
        </section>

        <section class="card">
            <div class="card-header">
                <h3>Доступные заявки</h3>
            </div>
            
            <div class="data-list">
                <div class="list-header">
                    <div>Услуга</div>
                    <div>Клиент</div>
                    <div>Дата</div>
                    <div>Действия</div>
                </div>
                <% orders.forEach(order => { %>
                <div class="list-item">
                    <div><%= order.serviceTitle %></div>
                    <div><%= order.Client ? order.Client.firstName + ' ' + order.Client.lastName : 'Не указан' %></div>
                    <div><%= new Date(order.createdAt).toLocaleDateString() %></div>
                    <div class="action-buttons">
                        <form action="/master/orders/<%= order.id %>/accept" method="post" style="display:inline;">
                            <button type="submit" class="action-btn accept-btn"><i class="fas fa-check"></i></button>
                        </form>
                    </div>
                </div>
                <% }) %>
            </div>
        </section>
    </main>
</body>
</html>